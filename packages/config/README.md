# Shared Config

This package exists to document shared repository configuration ownership. The live config files currently remain at the repository root for easier tool compatibility.

Owned by the QA/Ops Agent unless an issue explicitly delegates part of the configuration surface elsewhere.

